<html>
{{ block("head", "head.twig") }}
    <body>
    {{ block("Navigation", "navigation.twig") }}
    {% if  error == false %}
        <section>
            <div class="container">
                <div class="row">
                    <div class="col-lg-12 text-center">
                        <h2>
                        {% if result %}
                            Suppression(s) réalisée(s)
                        {% else %}
                            Erreur : Suppression(s) NON réalisée(s)
                        {% endif %}
                        </h2>
                    </div>
                </div>
            <div>
            <div class="container mt-3">
                <div class="row">
                    <div class="col-lg-12 text-center">
                        <div>
                            <a href="/">Retour à l'accueil</a>
                        </div>  
                    </div>
                <div>
            <div>
        </section>
        {% else %}
            <div class="h-75 col-lg-12 text-center">
                <h1 class="mt-3">Une erreur est survenue</h1>
            </div>        
        {% endif %}
    </body>
        {{ block("Footer", "footer.twig") }}
</html>